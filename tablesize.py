length = int(input('What is the length of the table in centimeteres?'))

width = int(input('What is the width of the table in centimeteres?'))

answer = (length*width)

print(f'The area of the table is {answer} cm sqaured!')
